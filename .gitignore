.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Session state — set per-agent per-session, not version-controlled
CURRENT_AGENT.md

# Live workflow folders contain client details — do not commit unless sanitized
workflows/*/
!workflows/_template/
!workflows/README.md
